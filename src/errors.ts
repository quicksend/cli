export enum APPLICATION_KEY_ERRORS {
  UNSET = "The application key is currently unset! Please run 'login --help' to learn how to set one."
}

export enum REMOTE_ERRORS {
  INVALID_PROTOCOL = "Invalid protocol specified. The protocol must be either 'http' or 'https'.",
  UNSET = "The remote is currently unset! Please run 'remote:set --help' to learn how to set one."
}
